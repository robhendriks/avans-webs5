[
  {
    "title": "Users",
    "description": "Allows you to access, create and remove users.",
    "url": "/users",
    "endpoints": [
      {
        "description": "Get all users.",
        "method": "GET",
        "features": ["HTML", "Pagination"],
        "parameters": {
          "page": { 
            "type": "Number", 
            "required": false,
            "description": "Page number"
          }
        },
        "examples": [
          { 
            "suffix":"<code>?page=1</code>", 
            "text": "Fetches the first page of users." 
          }
        ]
      },
      {
        "description": "Get a specific user.",
        "method": "GET",
        "url": "/:id",
        "parameters": {
          "id": { 
            "type": "String", 
            "required": true,
            "description": "User ID" 
          }
        },
        "examples": [
          { 
            "suffix":"/<code>1337</code>", 
            "text": "Fetches the user with ID <code>1337</code>." 
          }
        ]
      }
    ]
  },
  {
    "title": "Races",
    "description": "Allows you to access, create and remove races.",
    "url": "/races",
    "endpoints": [
      {
        "description": "Get all races.",
        "method": "GET",
        "features": ["HTML", "Pagination"],
        "parameters": {
          "page": { 
            "type": "Number", 
            "required": false,
            "description": "Page number"
          }
        },
        "examples": [{ 
          "suffix": "<code>?page=1</code>", 
          "text": "Fetches the first page of races." 
        }]
      },
      {
        "description": "Get a specific race.",
        "method": "GET",
        "url": "/:id",
        "parameters": {
          "id": { 
            "type": "String", 
            "required": true,
            "description": "Race ID" 
          }
        },
        "examples": [{ 
          "suffix":"/<code>1337</code>", 
          "text": "Fetches the race with ID <code>1337</code>." 
        }]
      }
    ]
  },
  {
    "title": "Waypoints",
    "description": "Allows you to access, create and remove waypoints.",
    "url": "/waypoints",
    "endpoints": [
      {
        "description": "Get all waypoints.",
        "method": "GET",
        "examples": [{ 
          "text": "Fetches all waypoints." 
        }]
      },
      {
        "description": "Get a specific waypoint.",
        "method": "GET",
        "url": "/:id",
        "parameters": {
          "id": { 
            "type": "String", 
            "required": true,
            "description": "Waypoint ID" 
          }
        },
        "examples": [{ 
          "suffix":"/<code>1337</code>", 
          "text": "Fetches the waypoint with ID <code>1337</code>." 
        }]
      }
    ]
  }
]
